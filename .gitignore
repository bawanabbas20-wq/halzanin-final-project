/.phpunit.cache
/node_modules
# /public/build  — committed so Hostinger gets compiled assets without Node.js
# /vendor        — committed so Hostinger gets dependencies without needing composer via SSH
/public/hot
/public/storage
/storage/*.key
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode

# Claude Code / AI tooling (local only — not part of the project)
/.claude/
/.agents/
/memory/
/skills-lock.json
